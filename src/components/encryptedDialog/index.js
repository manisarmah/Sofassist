import React from "react";
import "./styles.css";
const EncryptedDialog = () => {
  return (
    <div className="textEncrypted">
      <p>All chats are encrypted using standard AES</p>
    </div>
  );
};

export default EncryptedDialog;
